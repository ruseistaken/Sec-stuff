find / -type f -perm /4000 -ls 2>/dev/null SUID

find / -type f -perm /2000 -ls 2>/dev/null GUID

find / -type f -perm /6000 -ls 2>/dev/null SUID/GUID

https://gtfobins.org/

use SUID/sudo tabs depending on what privs are assigned

when going to clean log, ensure you have a copy and do the cleaning on the copy of the log

check for remote loggin by looking for rsyslog daemon
<facility>.<priority>

dot in path demo:
PATH=.:$PATH
cd /tmp (globally writable directory)
vim cat
  #!/bin/bash
  echo "meow meow meow"
chmod u+x cat
cat /etc/passwd
- file is named cat (same name as normal command)
- script will be run before the command is used depending on directory you are in (because of path variable)
